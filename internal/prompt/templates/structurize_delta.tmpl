You are processing reviewer "{{.ReviewerID}}" at debate round {{.Round}}.

Reviewer output in this round:
---
{{.RoundContent}}
---

{{- if .LedgerSummary}}
Previously tracked active issues for this reviewer:
{{.LedgerSummary}}
{{- else}}
There are no previously tracked issues for this reviewer.
{{- end}}

Extract only the delta for this round:
- "add": newly raised issues
- "retract": IDs explicitly withdrawn in this round
- "update": field updates for existing IDs

Output MUST follow this schema:

```json
{{.Schema}}
```

Rules:
- Output ONLY a fenced JSON block (no other text).
- Reuse only existing IDs in "retract" and "update".
- Do not invent IDs for "add"; local code assigns them later.
- If no changes exist, output empty arrays for add/retract/update.
- Keep file paths concrete and include line when available.
- Severity must be one of: critical, high, medium, low, nitpick.
{{- if .Language}}
- IMPORTANT: Write "title", "description", and "suggestedFix" in {{.Language}}.
  Keep JSON keys, severity values, and category values in English.
{{- end}}
